# AI QA attempt 001 — GraphPatch 操作数上限放宽（WORK-2026-046 / maxitems 修复）

```yaml
attestation_type: machine_attestation
actor_type: ai_agent
role_id: ai_qa_auditor
decision: pass
reviewed_commit: f8d673c
correlation_classification: correlated_review
human_signature: false
owner_acceptance: false
workspace_modified: false
network_used: false
```

## Decision

**PASS** — 0 P0 / 0 P1 / 0 P2 / 5 个 P3 观察（不阻塞）。这是对 WORK-2026-046
（`GraphPatch.operations.maxItems` 100→5000：canonical + 生成产物 + 契约/API 回归）
的职责隔离机器审查。完整报告见
`docs/test-reports/TR-20260815-007_patch-size-bound.md`（本文件为同一审查的
evidence 副本/摘要）。

## 红灯真值（隔离 worktree 实际重跑）

隔离 worktree（`git worktree add --detach` @ `bc08d8f`，detached，因分支已在主
工作树检出）中把 canonical JSON 与生成 Python 镜像的 `operations.maxItems`
5000→100 后：

- `test_graph_patch_accepts_large_operation_set` → **FAIL**
  `ContractValidationError: validation_failed: graph_patch at operations violates maxItems`
- `test_ai_draft_large_workspace_patch_exceeds_old_cap` → **FAIL**
  `422 {"code":"draft_invalid","rule":"maxItems","contract":"graph_patch","path":"operations"}`

还原 5000 后：契约 2 passed + API 1 passed。红灯声明与提交说明一致。

## Gates（本次执行，精确数字）

- 全量 pytest：**469 passed + 5 skipped**（live DeepSeek 无 key，如实 skipped）
- ruff check ./format --check：clean（116 文件；证据文件加入后复跑仍 clean）
- mypy scripts 16 文件；mypy --strict（incl. apps/desktop）40 文件：OK
- `scripts.validate_repository`：PASS（含 secret scan；证据文件加入后复跑仍 PASS）
- pnpm install --frozen-lockfile / peers check：OK；`pnpm check`：**53/53**
  （14 files）；`pnpm build`：OK（chunk 告警为既有提示）
- 契约 drift：`pnpm --filter @knowledge-tree/contracts-ts check`
  （`generate.mjs --check` + `tsc --noEmit`）exit 0（无漂移）
- 桌面 e2e（冻结 exe）：**18/18**

## Adversarial probes（15/15 Python）

- 契约层：5000 恰好接受；5001 拒绝 `rule=maxItems`；0 拒绝 `minItems`；150/2500
  接受；100（旧上限）仍接受（向后兼容）；重复 op_id → `duplicate_operation_id`；
  重复 target → `duplicate_operation_target`；user 起源 concept 非 null 置信度 →
  `user_confidence_must_be_null`。
- API 层：全库 240 操作 → 200 + `requires_confirmation`；全库无资源 → 422
  `no_resources`；无生成器 → 503 `ai_not_available`；单资源模式仍 200。
- 产物一致性：生成 Python 镜像 == canonical JSON（逐字段相等）、
  `operations.maxItems==5000`；TS 产物 schema_version=1、drift 门 exit 0。

## Frozen exe probes（9/9）

`dist/zhizhi/zhizhi.exe --no-window --port <空闲> --data-root <临时>`：
health 200；POST /workspaces 200；无 key `/ai-draft`（全库/单资源两模式）均 503
`ai_not_available`（fail-closed）；**120 操作补丁接受并提交**（200 applied，
revision_no=1，persisted 121=基线 1+120）；**5001 操作补丁 422
`patch_invalid/rule=maxItems`**（证明冻结 exe 内嵌 5000 上限且仍强制）；
进程终止后端口释放。exe 重建时间 3:01:14 晚于修复提交 f8d673c（3:00:26）；
实际体积 8,632,729 字节（简报称 ~18MB，已记录为观察）。

## Findings（P3 观察，不阻塞）

| Sev | 位置 | Finding | 处置 |
|-----|------|---------|------|
| P3 | 契约测试 | 上限+1 测试从 canonical JSON 读上限，对生成产物漂移不敏感 | 建议改读运行时产物；现由 drift 门 + P-G-001 兜底 |
| P3 | 集成测试 | no_resources 测试未钉死 `rule` | 建议补断言 |
| P3 | DoD 文档 | ENGINEERING_PLAN/TRACEABILITY_MATRIX/work-items README 更新未提交（实现侧写入，非 QA 所为） | 封存时由实现者/owner 提交 |
| P3 | 冻结产物 | exe 体积 8.2MB 而非 ~18MB | 以实测为准 |
| P3 | 性能 | 5000 操作提交未做基准 | 如需另开性能项 |

## 执行 vs 静态追踪

- **执行**：全部门禁、红灯重跑（隔离 worktree）、15/15 契约+API 探针、
  9/9 冻结 exe 探针、18/18 桌面 e2e。未修改产品代码、未提交任何 git 变更。
- **静态追踪**：`preview_graph_patch` 将 `ContractValidationError` 转为
  `GraphPatchError(validation_failed, {rule: maxItems})`；API 透出
  `draft_invalid/maxItems`（ai-draft）与 `patch_invalid/maxItems`（patches 提交
  门）；`_convert_domain_error` 映射确认。

## Disclosure

本报告为独立 AI QA 角色（与实现 Agent 角色隔离、同模型相关性）执行的机器审查，
是证据与工程发现的证明，**不是**人类签名、非 owner 接受。最终残余风险接受属于
workspace owner。
