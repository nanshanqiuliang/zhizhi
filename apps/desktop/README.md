# Desktop composition root

Tauri 2 / Rust desktop shell placeholder. `Cargo.toml` and Tauri configuration are intentionally absent until the Rust toolchain, sidecar contract, and WORK scope are approved. CI must not report a desktop compile pass while this manifest is absent.
