# TR-20260816-003 QA 报告：WORK-2026-051 知识树 PNG 导出

- test_run_id: `TR-20260816-003`
- reviewed_commit: `1154c80`（实现提交）+ build.spec 修复提交（PIL 打包排除移除）
- 工作项文档: `docs/work-items/WORK-2026-051_png-export.md`
- 判定: **PASS**（0 P0 / 0 P1 / 0 P2 / 1 P3 观察 + 1 P3 覆盖边界关闭记录）
- 角色隔离声明: 本报告由独立 QA 角色出具，与实现者角色分离（独立运行、独立工件）；
  **correlation 披露：`correlated_review`**——本 QA 与实现者（提交作者 Codex，AI 实现）
  同属自动化编排流程，可能同模型/供应商；`human_signature=false`、
  `owner_acceptance=false`（机器证明不冒充人类签名，最终残余风险接受权归工作区所有者）。

---

## 1. 红灯真值（真实运行，实现前）

```
uv run python -m pytest -q tests/integration/test_png_export.py tests/integration/test_mcp_bridge.py
    -> 8 failed, 10 passed（png_export 模块缺失 + 工具集断言 6≠7）
       （存档 logs/red-run-python.log）
pnpm --filter @knowledge-tree/web exec vitest run src/App.export.test.tsx
    -> 1 failed（getGraphImageDownloadUrl/按钮缺失）
       （存档 logs/red-run-web.log）
```

## 2. 绿灯与全部门禁（在 `1154c80` 上真实运行）

| 门禁 | 结果 |
|---|---|
| `test_png_export.py` | 7/7 passed（布局确定性+同层无重叠/小图 PNG magic+非空/空图占位/中文回退/端点 200 image/png + exports 落盘/未知工作区 404） |
| `test_mcp_bridge.py` | 含 export_png（exports 文件 + 图库 revision 不变）+ 工具集 7 + stdio 子进程冒烟 7 工具 |
| `App.export.test.tsx` | 1/1 passed（按钮触发 window.open 下载 URL） |
| `uv run python -m scripts.validate_repository` | PASS |
| `uv run ruff format --check packages scripts tests apps` | 123 files already formatted |
| `uv run ruff check .` | All checks passed |
| `uv run mypy scripts` | 16 source files 无问题 |
| `uv run python -m mypy --strict ...` | **43 source files** 无问题（+1 = png_export.py） |
| `uv run python -m pytest -q` | **500 passed, 5 skipped** in 26.24s |
| `pnpm check` | **19 files / 73 tests** 全绿 |
| `pnpm build` | OK（chunk-size 警告为既有问题） |
| `pnpm --filter @knowledge-tree/contracts-ts check` | exit 0 |
| `pnpm peers check` | PASS |

## 3. 冻结产物（真实运行）

- 首次冻结后冒烟失败：`ModuleNotFoundError: No module named 'PIL'`——`build.spec`
  `excludes` 中历史性排除了 PIL（避免 pypdf 可选引入）。修复：移除 PIL 排除并加注释。
- 修复后重建：`zhizhi.exe` onedir、`zhizhi-0.1.0-setup.exe`（28,922,730 字节，较上版
  +4.0MB 为 Pillow）、`zhizhi-0.1.0-portable.zip`（36,480,216 字节）。
- 冻结冒烟（`probes/frozen_png_smoke.py`，输出 `logs/frozen-smoke.log`）：
  **5/5 PASS**——7 工具枚举、export_png ok、PNG magic 校验、落盘 exports/、图库
  revision/concepts 不变。

## 4. 验收标准核对

| AC | 结果 |
|---|---|
| AC-1 小图合法非空 PNG | PASS |
| AC-2 空图占位不崩 | PASS |
| AC-3 中文字体回退不崩 | PASS |
| AC-4 layout_tree 确定性 + 同层无重叠 | PASS |
| AC-5 端点 200 image/png；MCP export_png path + 图库不变；Web 按钮下载 | PASS |
| 回滚 | 回退提交 + `uv remove pillow`；exports 文件可删 |

## 5. 发现与观察

- **P3-001（过程发现，已修复）**：build.spec 历史 PIL 排除导致首建冻结包缺 PIL——
  由冻结冒烟捕获（正是冒烟环节存在的意义），修复后复测通过。
- **P3-002（设计边界）**：导出为层次布局，未映射画布自由拖拽坐标（layout_items），
  工作项 Out of scope，后续增强。
- vite chunk >500KB 警告（既有）。

## 6. 结论

WORK-2026-051 验收标准全部满足；冻结产物链路验证通过。**判定 PASS**。
