# TR-20260815-010 QA 报告：WORK-2026-048 内置 MCP server（第 11 步切片 1：读 + AI 提议，零写工具）

- test_run_id: `TR-20260815-010`
- reviewed_commit: `944a996`（fix(mcp): ruff/mypy strict cleanups，实现提交 `fab4d70` + `944a996`）
- 分支: `feature/WORK-2026-048-mcp-server`
- 工作项文档: `docs/work-items/WORK-2026-048_mcp-server.md`
- 判定: **PASS**（0 P0 / 0 P1 / 0 P2 / 4 P3 观察，均非本变更引入的缺陷）
- 角色隔离声明: 本报告由独立 QA 角色出具，与实现者角色分离（独立运行、独立提示词、独立工件）；**correlation 披露：`correlated_review`** —— 本 QA 与实现者同属自动化编排流程，可能同模型/供应商（本 QA 运行于 DeepSeek 系模型）；`human_signature=false`、`owner_acceptance=false`（机器证明不冒充人类签名，最终残余风险接受权归工作区所有者）。
- 约束合规：未提交任何 git 变更；未修改产品代码（探针临时文件全部位于 QA 临时目录/evidence，未遗留于 apps/web/src 或 tests/）；所有断言基于真实运行输出。

---

## 1. 红灯真值（隔离 worktree @944a996，全部真实运行）

`git worktree add --detach E:\ztree-qa-worktree-010 HEAD` → worktree 内 `uv sync --locked --group dev`（mcp 1.29.0，满足 `mcp<2`）→ 将 `apps/api/mcp_server.py` 改名为 `mcp_server.py.bak`：

```
uv run python -m pytest -q tests/integration/test_mcp_bridge.py
```

**红灯：** `ERROR collecting tests/integration/test_mcp_bridge.py：ModuleNotFoundError: No module named 'apps.api.mcp_server'`（`1 error in 1.35s`，exit 2）——被审模块移除即测试无法收集，红灯成立。

**绿灯：** 还原文件名后同命令 → `7 passed, 1 warning in 1.55s`（exit 0）。

worktree 已 `git worktree remove --force` + `git worktree prune`（worktree list 仅剩主检出）。日志：`evidence/TR-20260815-010/logs/redlight-944a996.log`、`head-944a996.log`。

## 2. 全部门禁（HEAD 944a996 真实运行）

| 门禁 | 结果 |
|---|---|
| `uv sync --locked --group dev` | PASS（91 packages resolved） |
| `uv run python -m scripts.validate_repository` | PASS（skeleton/secret scan/graph+LLM 契约/review packet/v2 mock-replay） |
| `uv run ruff format --check packages scripts tests apps` | PASS（118 files already formatted） |
| `uv run ruff check .` | PASS（All checks passed） |
| `uv run mypy scripts` | PASS（16 source files） |
| `uv run python -m mypy --strict packages/contracts-py/src packages/domain/src packages/infrastructure/src apps/api apps/desktop` | PASS（**41 source files**，含 apps/desktop 与新增 mcp_server.py） |
| `uv run python -m pytest -q` | **476 passed, 5 skipped, 1 warning in 26.84s**（5 skip = live DeepSeek smoke 需显式 RUN_LIVE_LLM_TESTS=1 + key，符合预期；476 = 上轮 469 + 新增 7 桥测试） |
| `pnpm install --frozen-lockfile` | PASS（Already up to date） |
| `pnpm peers check` | PASS（No peer dependency issues found） |
| `pnpm check`（tsc -b + eslint + vitest） | PASS：**16 files / 64 tests passed** |
| `pnpm build` | PASS（vite built in 169ms；chunk>500kB 警告为既有提示） |
| contracts-ts drift（`pnpm --filter @knowledge-tree/contracts-ts check`） | PASS（generate.mjs --check && tsc --noEmit，exit 0） |

证据：`evidence/TR-20260815-010/gates/*.log`、`gate-summary.txt`。

## 3. 对抗探针（15 项脚本 + 真实运行，全部 PASS）

探针脚本 `evidence/TR-20260815-010/probes/qa_probe_mcp.py`（`uv run python` 执行），输出 `logs/probes-mcp.log`；首轮 13/15 中 P-012/P-013 两个 FAIL 均为**探针自身缺陷**（① `/api/workspaces` 返回 `{"workspaces":[...]}` 而非裸数组；② 无 key 断言在播种前调用返回 workspace_missing），修正探针后 **15/15 PASS**（首轮日志 `probes-mcp-run1.log` 保留备查）。

| ID | 探针 | 结果 |
|---|---|---|
| P-001 | 工具集精确 = {list_workspaces, read_workspace, preview_draft, validate_patch}，无 write/apply/submit/commit/save/delete/accept 类工具 | PASS |
| P-002 | preview_draft 单资源（注入生成器 + 导入 md，resource_id 给定）：patch.requires_confirmation=true / confirmed=false、生成器收到正确文本、图 revision_no 不变（未写库） | PASS |
| P-003 | preview_draft 无 key（无注入、无 ai.json、无 env）：单资源与全库均为 `{ok:false, code:ai_not_available, rule:key_required}`；其余调用仍正常 | PASS |
| P-004 | 未知工作区 / UUIDv7 目录无 db / db 无图：结构化 workspace_missing / workspace_corrupt，不抛协议异常 | PASS |
| P-005 | validate_patch 全分支：合法草案→status=requires_confirmation；confirmed:true 用户补丁→status=ready_to_apply（history_records=0，未写库）；actor 不符→patch_invalid/actor_context_mismatch；base_revision 冲突→patch_invalid/base_revision_mismatch；requires_confirmation=false→patch_invalid/confirmation_required | PASS |
| P-006 | list_workspaces 空数据根→[]（含噪声目录/ai.json 忽略）；播种后→1 个 | PASS |
| P-007 | preview_draft 单资源 PDF：parse_pdf_resource 自动解析（抽取 129,604 字符），草案 requires_confirmation、revision 不变 | PASS |
| P-008 | 全库草案但无资源→draft_invalid/no_resources | PASS |
| P-009 | 坏生成器输出全部结构化 fail-closed：无 operations→no_new_concepts；无 patch→patch_missing；坏 base→base_revision_mismatch（防御性 preview_graph_patch）；抛 DraftError→draft_invalid/simulated_ai_failure | PASS |
| P-010 | 源码 stdio 真实子进程会话：initialize + tools/list（4 工具）+ call list_workspaces/read_workspace/preview_draft（无 key fail-closed） | PASS |
| P-011 | 错误隔离：未知工作区报错后会话存活、参数级错误（isError）后会话存活、后续调用全部正常（进程不崩溃） | PASS |
| P-012 | 并发：`dist\zhizhi\zhizhi.exe --no-window --data-root <同根> --port <空闲>` sidecar 健康 + `/api/workspaces` 返回工作区 + MCP stdio 客户端连**同一数据根** list/read 正常 → 只读并发安全 | PASS |
| P-012b | sidecar 终止后端口释放、进程退出 | PASS |
| P-013 | 冻结 exe（2026-08-16 14:16:53 重建，11,565,348 B）`--mcp-stdio --data-root <临时>`：initialize + tools/list（精确 4 工具）+ 空根 [] + 会话存活期间播种 → list 1 个 + read_workspace 返回图 + 无 key 结构化 ai_not_available；关闭会话后 exe 进程数 before=0 during=1 **after=0（无残留）** | PASS |
| P-014 | `python -m apps.api.mcp_server --help` 正常（--data-root 参数） | PASS |

## 4. 发现（按严重级）

- **P0**：无。
- **P1**：无。
- **P2**：无（未发现本变更引入的缺陷）。
- **P3（4 项观察，均不阻塞 PASS）**：
  1. **validate_patch 对 actor 不符返回 `patch_invalid` 而非任务探针规格预期的 `permission_denied`**（P-005 实测 `{ok:false, code:patch_invalid, rule:actor_context_mismatch}`）。MCP 工具错误命名空间统一为 {patch_invalid, draft_invalid, workspace_*, ai_not_available}，rule 携带细节；满足工作项 AC-4「越权补丁返回稳定错误、不写库」。属行为命名差异，非缺陷。
  2. **mcp SDK 1.29 + pydantic-settings 启动警告**：`IncompleteFieldDefinitionWarning: Field 'lifespan' ...`（上游 SDK 内部 forward-ref 警告，走 stderr 不污染 stdio；pytest 全量同源 1 warning）。无功能影响；依赖已锁 `mcp<2`。
  3. **真实 LLM 草案路径未在本 QA 启用**（无 key）：preview_draft 的 AI 生成链路只验证到结构化 `ai_not_available` fail-closed 边界；真实 DeepSeek 链路由既有 476 测试与 live 门禁（RUN_LIVE_LLM_TESTS=1 + key 显式启用）覆盖。本 QA 用注入生成器验证了 MCP 工具对生成结果的契约（requires_confirmation/confirmed=false/防御性 preview_graph_patch）。
  4. **sidecar 应用日志捕获为空**：windowed bootloader 下 --no-window 模式的 stderr 日志未写入重定向文件（P-012 的功能断言经 HTTP health/workspaces 实测通过）；--mcp-stdio 模式 stdout 纯净已由 P-013 协议实测证明。

## 5. 验收标准对照（docs/work-items/WORK-2026-048_mcp-server.md）

- AC-1（`--mcp-stdio` 启动后标准 MCP 客户端完成 initialize + tools/list + tools/call）：P-010（源码）与 P-013（冻结 exe）PASS。
- AC-2（工具集精确 4 个、无写工具）：P-001/P-010/P-013 PASS。
- AC-3（preview_draft 返回 requires_confirmation 草案且不落库；无 key 结构化 ai_not_available）：P-002/P-003/P-007 PASS（revision 不变 + history_records=0）。
- AC-4（validate_patch 对越权补丁返回稳定错误、不写库）：P-005 全分支 PASS。
- 错误和恢复路径（坏数据根/未知工作区结构化错误而非崩溃）：P-004/P-011/P-013（播种前调用 fail-closed）PASS。
- TC-MCP-006/007/008：stdio 子进程协议冒烟（P-010）、全部门禁（第 2 节）、冻结 exe --mcp-stdio（P-013）PASS。

## 6. 结论

判定 **PASS**。WORK-2026-048（第 11 步切片 1）的红灯→绿灯闭环（隔离 worktree 改名模块→collection error→还原→7 passed）、12 项全部门禁（pytest 476+5、mypy strict 41 files、Web 64/64、构建与契约 drift 全绿）、15/15 对抗探针（含真实 stdio 协议、冻结 exe 生命周期无残留进程、sidecar+MCP 同数据根只读并发安全、错误隔离不崩溃）全部通过；未发现本变更引入的缺陷。零写工具硬约束在源码层与冻结产物层均实测成立。P3 观察 4 项均为命名/边界/上游警告类，不影响本切片交付。机器 QA 证据不冒充人类签名（`human_signature=false`、`owner_acceptance=false`），最终残余风险接受权归工作区所有者。

- 证据目录：`evidence/TR-20260815-010/`（manifest.json / checksums.sha256 / commands.txt / environment.json / gate-summary.txt / qa-attempt.md / gates / logs / probes）
- 复查方式：`git -C <repo> diff 37d0988 944a996` 复核实现范围；重跑 `evidence/TR-20260815-010/gates/run_python_gates.ps1`、`run_pnpm_gates.ps1` 与 `uv run python evidence\TR-20260815-010\probes\qa_probe_mcp.py` 复核结果。
