# RB-PROV-001 演练报告 — 2026-08-14

```yaml
runbook: RB-PROV-001
status: drilled（adapter + live smoke + 金标已演练；金标质量门与 QA 批准待后续）
executed_at: 2026-08-14T16:55:00+08:00
build: feature/WORK-2026-008-deepseek-adapter-live
related: [WORK-2026-007, WORK-2026-008, EVAL-LLM-001, NFR-2026-007, NFR-2026-008]
```

本报告把 `RB-PROV-001.md` 的定位步骤与错误处置表逐条对照到可重复测试与真实 smoke
证据。所有离线证据用脱敏 fixture 或 mock 故障注入，无网络、无密钥；live 证据在
`RUN_LIVE_LLM_TESTS=1` + `DEEPSEEK_API_KEY` 门控下运行（约 817 + 1338 token，费用远低于 3 元）。

## 定位步骤核对

| Step | 演练动作 | 预期信号 | 结果 | 证据 |
|---:|---|---|---|---|
| 1 | 判断数据/安全/副作用风险 | 明确影响范围 | PASS：LLM 只产出草案，无写库/改锁路径 | 架构（AI output is untrusted draft） |
| 2 | 核对 build/环境/config fingerprint | 与 manifest 一致 | PASS：`capability_fingerprint` 稳定；config 经 repository 门校验 | `test_capability_fingerprint_is_stable_and_sensitive_to_change`；validator |
| 3 | 查看 model router 选择 | deployment/capability 满足 | PASS：未知 deployment 失败关闭；缺能力发送前拒绝 | `test_deployment_resolution_uses_vendor_model_ids`；`test_capability_missing_is_rejected_before_request` |
| 4 | 核对 secret 状态与 endpoint host | secret present、host allowlist | PASS：密钥仅 env 传入、不落盘；host allowlist 校验在 repository 门 | secret scan（无命中）；`validate_llm_semantics` |
| 5 | 沿 trace 找首个失败 span | `provider.generate` 子边界明确 | PASS：稳定错误码 + `model_run_id`/`provider_response_id` | `test_error_details_never_contain_prompt_or_content` |
| 6 | 按 HTTP/网络/stream/schema 分类 | 映射稳定错误码 | PASS：401/402/403/422/429/500/503 → 对应稳定码 | `test_map_deepseek_http_error`（8 状态） |
| 7 | DeepSeek thinking/tool replay | opaque state 回传 | PASS：reasoning_content 工具轮回传、丢失返回 `provider_continuation_lost` | `test_build_chat_request_tool_round_replays_reasoning_continuation`；`test_continuation_lost_when_reasoning_state_missing` |
| 8 | 用脱敏 fixture 离线复现 | 稳定复现 mapper/parser | PASS：SSE 断流/keep-alive/`[DONE]`、tool 参数、截断 JSON | `test_sse_stream_*`；`test_truncated_json_text_fails_schema_as_provider_schema_failed` |
| 9 | 检查 retry/fallback/budget | 次数有界、原因获批、fresh run | PASS：429 有界重试、401 立即熔断不重试、ModelRunner 仅回退瞬态码、金额/attempt 预算超限 `budget_exceeded` | `test_adapter_retries_then_succeeds_on_429`；`test_adapter_does_not_retry_on_401`；`test_runner_*`；`test_adapter_enforces_cost_budget` |
| 10 | 写失败测试并修复 | 红灯→绿 | PASS：本轮新增金额预算与 fallback 测试均先实现后转绿 | `test_cost_budget_raises_when_exceeded` 等 |

## 错误处置表核对

| 错误 | 快速缓解 | 演练结果 |
|---|---|---|
| `provider_capability_missing` | 隔离 deployment | `test_capability_missing_is_rejected_before_request` |
| `provider_auth_failed` | 关闭真实调用，立即熔断不重试 | `test_adapter_does_not_retry_on_401`；`test_auth_failure_opens_circuit_immediately` |
| `provider_balance_exhausted` | 立即熔断 | `test_map_deepseek_http_error`（402） |
| `provider_rate_limited` | 有界退避后按 policy 回退 | `test_adapter_retries_then_succeeds_on_429`；`test_runner_falls_back_on_rate_limited` |
| `provider_unavailable`/`provider_connection_failed` | 有界重试 | `test_map_deepseek_http_error`；`test_runner_raises_when_all_adapters_fail` |
| `provider_schema_failed` | 丢弃结果，不创建草案 | `test_truncated_json_text_fails_schema_as_provider_schema_failed` |
| `provider_continuation_lost` | 从 checkpoint 重跑 | `test_continuation_lost_when_reasoning_state_missing` |
| `provider_stream_incomplete` | 丢弃 partial | `test_sse_stream_break_reports_stream_incomplete` |

## 恢复与验证核对

- 离线 fixture 先行（TC-LLM-001..009 mock + TC-DS-001..005）→ 受控 live smoke 5/5（文本/JSON/thinking/tool/stream）→ 金标 EVAL-LLM-001 基线（概念抽取 recall 0.133、关系准确率 0.667、带引用回答通过；成本 ~$0.0012）。
- half-open 恢复：`test_circuit_breaker_state_machine`（closed→open→half-open→closed）。
- 脱敏：错误 details 与 fixture 无正文/密钥/reasoning_content（secret scan 无命中）。

## 结论与遗留

- 结论：`RB-PROV-001` 的定位与错误处置路径已通过演练核对（离线 fixture + live smoke + 金标基线）。
- 遗留：金标质量门（EVAL-LLM-001 的 recall/accuracy 阈值批准）、`RB-PROV-001` 完整批准与
  DeepSeek deployment `enabled: true` 仍待 owner/QA 批准；本报告是开发侧演练，非独立 QA 签字。
